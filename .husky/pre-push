#!/usr/bin/env sh
# Mandatory pre-push check. Full production build to catch Next-specific
# errors (route config, static generation, missing dynamic exports) that
# tsc + lint do not see. Runs once per push, not once per commit.

set -e

echo "[pre-push] Running Next.js production build..."
npm run build

echo "[pre-push] Build passed. Safe to push."
