#!/usr/bin/env sh
# Mandatory pre-commit checks. Fast: runs lint + type-check only.
# The full build runs on pre-push (slower, rarer).

set -e

echo "[pre-commit] Running ESLint..."
npm run lint

echo "[pre-commit] Running TypeScript type check..."
npx tsc --noEmit

echo "[pre-commit] All checks passed."
